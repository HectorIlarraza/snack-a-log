// const confirmHealth = (req, res, next) => {
//     const { body } = req;
    
//     if(body.protein >= 5 || body.fiber >=5 && body.added_sugar <5){
//          body.is_healthy = true
//     }else if(body.protein !== Number && body.fiber !== Number && body.added_sugar !== Number){
//         body.is_healthy = null
//     }
//     else{
//         body.is_healthy = false
//     }
//     next()
// };

// module.exports = confirmHealth;
